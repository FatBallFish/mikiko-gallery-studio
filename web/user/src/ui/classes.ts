import { rdShell, button, form, state, pill, card, text } from './redesign-classes'

// Deprecated: kept for backward compatibility only.
// All new code should import from `redesign-classes.ts` directly.
export const userShell = rdShell
export const userButton = button
export const userForm = form
export const userState = state
export const userPill = pill
export const userCard = card
export const userText = text