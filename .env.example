# [中文] 此文件仅用于提示旧版部署的迁移入口，应用不会自动读取项目根目录的 .env。
# [English] This file only points legacy deployments to the new layout; the application does not auto-load a root .env file.
#
# [中文] 请从双语模板创建唯一的运行时配置文件：
# [English] Create the single runtime configuration file from the bilingual template:
#
#   mkdir -p config
#   cp config/runtime.env.example config/runtime.env
#
# [中文] API 和 Worker 默认读取 ./config/runtime.env。仅当服务管理器无法设置工作目录时，才使用 APP_ENV_FILE 覆盖路径。
# [English] API and Worker read ./config/runtime.env by default. Use APP_ENV_FILE only when a service manager cannot set the working directory.
