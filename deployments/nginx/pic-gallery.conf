upstream pic_gallery_api { server api:8080; }
server {
  listen 80;
  server_name _;
  client_max_body_size 20m;
  location / { proxy_pass http://pic_gallery_api; proxy_set_header Host $host; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; }
}
