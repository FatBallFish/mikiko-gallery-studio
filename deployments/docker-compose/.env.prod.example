# Legacy Docker env files are retired.
# deployctl generates the portable, bilingual runtime configuration at:
#   ./config/runtime.env
#
# Example:
#   deployctl install --mode docker --profile full --topology single --yes
#
# Compose reads deployment values from config/runtime.env. Do not maintain a
# second copy of application secrets in this file.
